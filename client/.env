# The lobby/auth server owns port 3000; the client dev server takes 3002.
# Reach everything as `localhost` (never 127.0.0.1) so the session cookie
# set by the lobby also rides to the game server.
PORT=3002
