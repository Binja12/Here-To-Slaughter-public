// ---------------------------------------------------------------------------
// Item tasks — steps that act on an item card.
//
// Equipping follows the layout in magic-tasks.ts: the mechanic in an abstract
// base, with the IAction in `actions/` and the ITask here extending it.
// ---------------------------------------------------------------------------

export {}
